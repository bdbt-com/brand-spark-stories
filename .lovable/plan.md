In `src/pages/Home.tsx` (lines 532–541, "Ready To Replace Daily Drifts With Daily Wins?" section), swap the two buttons' variants:

- **Start With The Free Foundation Blueprint** → add `variant="outline"` (becomes black with gold border)
- **Browse Courses** → remove `variant="outline"` (becomes solid gold)

Link targets, text, sizes, italics, and ordering stay exactly the same. No other files touched.